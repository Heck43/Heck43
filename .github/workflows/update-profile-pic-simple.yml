name: Update Profile Picture from ImgBB

on:
  schedule:
    - cron: "0 * * * *"  # Каждый час
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl
        
      - name: Get Random Image from ImgBB Album
        run: |
          # ImgBB API ключ
          API_KEY="8210be0791dd4e570500e79ed8d8d54f"
          
          # ID альбома (из ссылки https://ibb.co/album/6R58df)
          ALBUM_ID="6R58df"
          
          # Парсим альбом и получаем список картинок
          echo "Получаем список картинок из альбома..."
          
          # Парсим HTML альбома постранично и собираем ссылки на просмотр из data-object
          VIEWS=""
          PAGE_NUM=1
          MAX_PAGES=50
          SEEK=""
          PREV_PAGE_VIEWS=""
          while [ "$PAGE_NUM" -le "$MAX_PAGES" ]; do
            PAGE_URL="https://ibb.co/album/$ALBUM_ID?page=$PAGE_NUM"
            if [ -n "$SEEK" ]; then
              PAGE_URL="${PAGE_URL}&seek=${SEEK}"
            fi
            PAGE=$(curl -sL -A "Mozilla/5.0" "$PAGE_URL")
            PAGE_VIEWS=$(echo "$PAGE" \
              | grep -oP 'url_viewer%22%3A%22https%3A%5C%2F%5C%2Fibb.co%5C%2F[^%]+' \
              | sed 's/^url_viewer%22%3A%22//' \
              | python3 -c 'import sys,urllib.parse; print("\n".join(urllib.parse.unquote(l.strip()).replace("\\\\","") for l in sys.stdin if l.strip()))')
            PAGE_COUNT=$(echo "$PAGE_VIEWS" | sed '/^$/d' | wc -l | tr -d ' ')
            if [ "$PAGE_COUNT" -eq 0 ]; then
              echo "Страница $PAGE_NUM: найдено 0, стоп"
              break
            fi
            if [ "$PAGE_VIEWS" = "$PREV_PAGE_VIEWS" ]; then
              echo "Страница $PAGE_NUM: совпадает с предыдущей, стоп"
              break
            fi
            VIEWS="${VIEWS}${PAGE_VIEWS}"$'\n'
            SEEK=$(echo "$PAGE" | grep -oP 'data-seek="\\K[^"]+' | head -n 1 || true)
            if [ -z "$SEEK" ]; then
              SEEK=$(echo "$PAGE" | grep -oP "data-seek='\\K[^']+" | head -n 1 || true)
            fi
            echo "Страница $PAGE_NUM: найдено $PAGE_COUNT, seek=${SEEK:-нет}"
            PREV_PAGE_VIEWS="$PAGE_VIEWS"
            PAGE_NUM=$((PAGE_NUM + 1))
          done
          VIEWS=$(echo "$VIEWS" | sed '/^$/d' | sort -u)
          
          # Считаем количество ссылок на просмотр
          VIEW_COUNT=$(echo "$VIEWS" | wc -l | tr -d ' ')
          echo "Найдено $VIEW_COUNT ссылок на просмотр"
          
          if [ "$VIEW_COUNT" -eq 0 ]; then
            echo "Не удалось получить картинки из альбома"
            exit 1
          fi
          
          # Проверяем выбранную картинку, чтобы отсеять битые/не-изображения
          is_valid_image() {
            local url="$1"
            local headers status ctype clen
            headers=$(curl -sIL -A "Mozilla/5.0" "$url" | tr -d '\r')
            status=$(echo "$headers" | awk 'tolower($0) ~ /^http/ {code=$2} END{print code}')
            ctype=$(echo "$headers" | awk -F': ' 'tolower($1)=="content-type" {print tolower($2); exit}')
            clen=$(echo "$headers" | awk -F': ' 'tolower($1)=="content-length" {print $2; exit}')
            if [ "$status" != "200" ]; then
              return 1
            fi
            if [ -n "$ctype" ] && [[ "$ctype" != image/* ]]; then
              return 1
            fi
            if [ -n "$clen" ] && [ "$clen" -gt 10000000 ]; then
              return 1
            fi
            return 0
          }
          
          # Выбираем рандомную ссылку на просмотр и вытаскиваем прямую ссылку на картинку
          NEW_IMAGE=""
          ATTEMPTS=0
          MAX_ATTEMPTS=5
          while [ "$ATTEMPTS" -lt "$MAX_ATTEMPTS" ]; do
            RANDOM_LINE=$((RANDOM % VIEW_COUNT + 1))
            VIEW_URL=$(echo "$VIEWS" | sed -n "${RANDOM_LINE}p")
            VIEW_PAGE=$(curl -sL -A "Mozilla/5.0" "$VIEW_URL")
            CANDIDATE=$(echo "$VIEW_PAGE" | grep -oP '<meta property="og:image" content="\K[^"]+')
            if [ -n "$CANDIDATE" ] && is_valid_image "$CANDIDATE"; then
              NEW_IMAGE="$CANDIDATE"
              break
            fi
            ATTEMPTS=$((ATTEMPTS + 1))
          done
          
          if [ -z "$NEW_IMAGE" ]; then
            echo "Не удалось выбрать валидную картинку за $MAX_ATTEMPTS попыток"
            exit 1
          fi
          
          # Обходим кэш GitHub для изображений
          CACHE_BUST=$(date +%s)
          NEW_IMAGE="${NEW_IMAGE}?v=${CACHE_BUST}"
          
          echo "Выбрана картинка: $NEW_IMAGE"
          
          # Сохраняем текущую картинку из README
          CURRENT_IMAGE=$(grep -oP '<img src="\K[^"]+(?=" width="350")' README.md)
          echo "Текущая картинка: $CURRENT_IMAGE"
          
          # Если новая картинка такая же как текущая, выбираем другую
          if [ "$NEW_IMAGE" == "$CURRENT_IMAGE" ] && [ "$VIEW_COUNT" -gt 1 ]; then
            echo "Выбрана та же картинка, выбираем другую..."
            RANDOM_LINE=$((RANDOM % VIEW_COUNT + 1))
            VIEW_URL=$(echo "$VIEWS" | sed -n "${RANDOM_LINE}p")
            VIEW_PAGE=$(curl -sL -A "Mozilla/5.0" "$VIEW_URL")
            NEW_IMAGE=$(echo "$VIEW_PAGE" | grep -oP '<meta property="og:image" content="\\K[^"]+')
            echo "Новая картинка: $NEW_IMAGE"
          fi
          
          # Заменяем картинку в README (ищем любую картинку с width="350")
          sed -i "s|<img src=\"[^\"]*\" width=\"350\"|<img src=\"$NEW_IMAGE\" width=\"350\"|g" README.md
          
          echo "Картинка обновлена!"
          
      - name: Commit and Push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          if git diff --staged --quiet; then
            echo "Нет изменений"
          else
            git commit -m "🎨 Обновлена картинка профиля (Loona)"
            git push
          fi
